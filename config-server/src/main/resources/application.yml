server:
  port: 1000

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ArturV153/ecommerceproject.git
          default-label: main
          searchPaths:
            - configuration
            - configuration/user-service
            - configuration/order-service
            - configuration/product-service